"use client";

import Dashboard from "@/components/dashboard/Dashboard";

/* Dashboard PEV de routine — shell : chargement → bienvenue → accueil 4 onglets → vue onglet. */
export default function Page() {
  return <Dashboard />;
}
